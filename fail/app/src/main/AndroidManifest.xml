<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utbm.lo52.fail">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScoreActivity"></activity>
        <activity
            android:name=".RunnerInscriptionActivity"
            android:label="@string/title_activity_runner_inscription"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RunnerStateActivity" />
        <activity android:name=".RaceStateActivity" />
        <activity android:name=".ArchiveActivity" />
        <activity android:name=".RaceActivity" />
        <activity android:name=".PlayerRegistrationActivity" />
        <activity android:name=".TeamRegistrationActivity" />
        <activity
            android:name=".DummyActivity"
            android:label="@string/title_activity_dummy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>